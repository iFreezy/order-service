name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      CI: true
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          # версия берётся из go.mod, чтобы CI не отставал от требований зависимостей
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Build
        run: go build -o ./bin/ -v ./...

      - name: Test
        run: make test

  linter:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          # версия берётся из go.mod, чтобы CI не отставал от требований зависимостей
          go-version-file: go.mod
          cache: false
      - name: golangci-lint
        # v6 — последняя версия экшена, поддерживающая golangci-lint v1.x;
        # v7+ работает только с golangci-lint v2 (другой формат .golangci.yml)
        uses: golangci/golangci-lint-action@v6.5.2
        with:
          # Require: The version of golangci-lint to use.
          # When install-mode is binary (default) the value can be v1.2 or v1.2.3 or latest to use the latest version.
          # When install-mode is goinstall the value can be v1.2.3, latest, or the hash of a commit.
          version: v1.64.8

          # Собираем линтер из исходников тем же Go, что и проект.
          # Готовый бинарник golangci-lint собран с go1.24 и не может
          # тайпчекать зависимости, которые требуют go1.25 (gin, pgx, x/net).
          install-mode: goinstall

          # Optional: working directory, useful for monorepos
          # working-directory: somedir

          # Optional: golangci-lint command line arguments.
          #
          # Note: By default, the .golangci.yml file should be at the root of the repository.
          # The location of the configuration file can be changed by using --config=
          args: --timeout=30m --config=./.golangci.yml

          # Optional: show only new issues if it's a pull request. The default value is false.
          # only-new-issues: true

          # Optional: if set to true, then all caching functionality will be completely disabled,
          #           takes precedence over all other caching options.
          # skip-cache: true

          # Optional: if set to true, then the action won't cache or restore ~/go/pkg.
          # skip-pkg-cache: true

          # Optional: if set to true, then the action won't cache or restore ~/.cache/go-build.
          # skip-build-cache: true

          # Optional: The mode to install golangci-lint. It can be 'binary' or 'goinstall'.
          # install-mode: "goinstall"